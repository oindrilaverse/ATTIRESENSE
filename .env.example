# In this project, API keys are input via the UI and stored in LocalStorage.
# However, if this project transitions to a Node.js backend to hide the key,
# this is the structure of the required environment variables.

GEMINI_API_KEY=your_google_gemini_api_key_here